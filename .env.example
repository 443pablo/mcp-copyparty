# copyparty MCP Server Configuration Example
# Copy this file to .env and update with your values

# Required: URL of your copyparty server
COPYPARTY_URL=http://localhost:3923

# Optional: Authentication credentials for copyparty
# Leave blank if your server doesn't require authentication
COPYPARTY_USERNAME=
COPYPARTY_PASSWORD=

# Optional: Environment setting (development/production)
ENVIRONMENT=development

# Optional: Port for the MCP server (default: 8000)
# PORT=8000
